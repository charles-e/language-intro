title: Our Amazing Team

hello.html = greet('world'):

teamByName = mapKeys(team.yaml, =name):
greetings = map(teamByName, =greet(name), '', '.html'):

team = map(teamByName, person.ori, '', '.html'):

index.html = index.ori():
